-- Seraph seed data for local development
-- Runs automatically on docker compose up

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- See each service's own migration files for schema.
-- This file seeds initial test data after migrations run.

-- (Populate after running migrate-up)
